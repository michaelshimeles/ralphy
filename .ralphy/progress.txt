Task: Add weighted distribution strategy to get_engine_for_agent()

Implementation completed:

1. Added multi-engine configuration variables (ralphy.sh:88-97):
   - ENGINES array: List of engines to use
   - ENGINE_DISTRIBUTION: Strategy selector (round-robin, weighted, random, fill-first)
   - ENGINE_WEIGHTS: Associative array mapping engine names to weights
   - EXPANDED_ENGINES: Expanded array for weighted distribution
   - ENGINE_AGENT_COUNT, ENGINE_COSTS, ENGINE_SUCCESS, ENGINE_FAILURES: Tracking arrays
   - VALID_ENGINES: Array of valid engine names

2. Implemented expand_engines_by_weight() function (ralphy.sh:134-161):
   - Expands engines array based on their weights
   - Each engine appears N times in EXPANDED_ENGINES where N is its weight
   - Defaults to weight of 1 if not specified
   - Example: {claude:2, opencode:1} creates [claude, claude, opencode]

3. Implemented get_engine_for_agent() function (ralphy.sh:163-231):
   - Takes agent number as parameter
   - Returns engine name based on selected distribution strategy
   - Supports multiple distribution strategies:
     * round-robin: Simple modulo distribution across engines
     * weighted: Cycles through expanded array (engines repeated by weight)
     * random: Random selection from engines array
     * fill-first: Placeholder (falls back to round-robin)
   - Handles edge cases (no engines, single engine)

Weighted Distribution Algorithm:
- The key innovation is expanding the engines array by weight
- Example with weights {claude:3, opencode:2, cursor:1}:
  * EXPANDED_ENGINES = [claude, claude, claude, opencode, opencode, cursor]
  * Agent 0 → claude (index 0)
  * Agent 1 → claude (index 1)
  * Agent 2 → claude (index 2)
  * Agent 3 → opencode (index 3)
  * Agent 4 → opencode (index 4)
  * Agent 5 → cursor (index 5)
  * Agent 6 → claude (index 0, wraps around)
- This ensures proportional distribution matching the specified weights

Testing:
- Created test_weighted_distribution.sh with comprehensive test cases
- Created verify_weighted_distribution.sh for syntax and structure verification
- All verifications pass successfully

Files modified:
- ralphy.sh: Added configuration variables and functions

Files created:
- test_weighted_distribution.sh: Comprehensive test suite (requires bash 4.0+)
- verify_weighted_distribution.sh: Verification script (works on bash 3.2+)

The implementation is complete and ready for integration with the rest of the multi-engine system.
